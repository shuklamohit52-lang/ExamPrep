# ExamPrep Backend (Node + Express)

Provides APIs for:
- tests
- free PDFs/eBooks resources
- daily current affairs feed
- auth placeholder endpoint
- subscription checkout/webhook
- admin upload routes

## Run
```bash
cd backend
npm install
npm run start
```

Server default: `http://localhost:8080`
Admin panel: `http://localhost:8080/admin`
